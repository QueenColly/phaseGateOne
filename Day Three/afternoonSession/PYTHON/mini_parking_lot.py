def get_slots(slots):
    total = 20
    available = 0
    filled = 0
    for park in range(len(slots)):
        if slots[0] = 1:
            filled += 1
            available -= 1

    return slots

match(slots):

    case"available": return 20
    case "filled": return 0
    case"available": return 1
